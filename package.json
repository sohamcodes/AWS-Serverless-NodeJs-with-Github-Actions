{
  "name": "serverless-nodejs-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "sudo serverless offline --stage dev",
    "deploy": "yarn run serverless deploy --stage prod --region us-east-2",
    "remove": "sudo serverless remove --stage prod --region us-east-2",
    "generate": "drizzle-kit generate --config=drizzle.config.js",
    "migrate": "tsx src/cli/migrator.js"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.670.0",
    "@neondatabase/serverless": "^0.10.1",
    "aws-sdk": "^2.1691.0",
    "drizzle-kit": "^0.25.0",
    "drizzle-orm": "^0.34.1",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0",
    "tsx": "^4.19.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^14.3.2"
  }
}
